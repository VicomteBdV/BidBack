# Project-scoped delegation; models and execution permissions inherit from the caller.
# Standalone role files in .codex/agents/ are discovered by supported Codex clients.
[agents]
enabled = true
max_concurrent_threads_per_session = 3
